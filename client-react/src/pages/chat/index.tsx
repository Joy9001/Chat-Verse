import { Outlet } from '@tanstack/react-router'

export default function ChatPage() {
	return (
		<div className="min-h-screen">
			<div className="text-2xl font-bold p-4">Chat Page (Coming Soon)</div>
			<Outlet />
		</div>
	)
}
